# 음료 얼려 먹기:
# n * k 크기의 얼음 틀, 구멍은 0 칸막이 부분은 1 
# 구멍이 뚫린 부분이 상하좌우로 붙어 있는 경우 연결된 것으로 간주
# 얼음틀 모양이 주어졌을 때 생성되는 아이스크림 개수?

# 첫번째 줄: 세로 길이 n, 가로 길이 k (n*k 행렬)
# 두번째 줄부터 얼음 틀 2차원 배열

# 즉, 연결 요소 찾기 문제

# dfs
# 특정 지점의 주변 상하좌우를 살펴본 뒤에 주변 지점 중 값이 0이면서 아직 방문하지 않은 지점이 있다면 해당 지점 방문
# 방문한 지점에서 다시 상주좌우 살펴서 방문하는 과정 반복 -> 연결된 모든 지점 방문으가능
# 모든 노드에 대해 과정 반복하여 방문하지 않은 지점의 수 카운트

# dfs로 특정 노드 방문, 연결 모든 노드 방문
def dfs(x,y):
    # 주어진 범위를 벗어나는 경우 즉시 종료
    if x <= -1 or x >= n or y <=-1 or y >= m :
        return False
    
    # 현재 노드를 방문하지 않았다면 
    if graph[x][y] == 0:
        # 해당 노드 방문 처리
        graph[x][y] = 1
        
        # 상하좌우 위치들 모두 재귀적으로 호출
        dfs(x-1, y)
        dfs(x, y-1)
        dfs(x+1, y)
        dfs(x, y+1)
        return True
    return False

# n,k을 공백 기준 구분하여 입력 받기
n, m = map(int, input().split())
# 2차원 리스트 맵 정보 입력 받기
graph = []
for i in range(n):
    graph.append(list(map(int, input())))

result = 0 
for i in range(n):
    for j in range(m):
        if dfs(i,j) == True: # 처음 방문할 때만
            result += 1 # 카운트
print(result)
