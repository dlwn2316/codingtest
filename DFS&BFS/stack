# 스택 자료 구조: 선입후출(박스처럼)
# 표준라이브러리 없이 리스트 자료형 사용
# 삽입과 삭제(append, pop)

stack = []
stack.append(5) # 앞부터 삽입
stack.append(2)
stack.append(3)
stack.append(7)
stack.pop() # 뒤부터 삭제
stack.append(1)
stack.append(4)
stack.pop()

print(stack) # 앞에서부터 출력
print(stack[::-1]) # 뒤에서부터 
